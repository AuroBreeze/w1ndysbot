# 违禁词监控模块

基于权重的违禁词监控模块，支持自定义违禁词

## 默认广告规则

首次创建 `data/GroupBanWords/global_data.db`，或旧数据库第一次升级到本版本时，会向全局词库写入一组权重为 100 的默认广告正则规则，覆盖刷单、高薪兼职、微信引流、进群领资料、作业论文代写、培训办卡推广、低价课程和返利二维码等常见场景。

默认规则只初始化一次。管理员后续修改或删除规则时，机器人重启不会自动恢复；数据库中已经存在的同名规则也不会被覆盖。群专属规则仍优先于全局规则。
